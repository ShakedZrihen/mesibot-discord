import { Box, styled } from "@mui/material";

export const PartyViewContainer = styled(Box)({
  display: "flex",
  alignItems: "center",
  justifyContent: "center",
  marginTop: "17vh",
  flexDirection: "column"
});
