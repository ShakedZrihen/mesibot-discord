export enum OptionType {
  string = 3
}

export enum SupportedCommands {
  play = "play",
  pause = "pause",
  resume = "resume",
  join = "join",
  ping = "ping",
  clearQueue = "clear-queue",
  deleteSong = "delete-song",
  skip = "skip"
}
